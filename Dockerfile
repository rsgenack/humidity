# Use an official Node.js runtime as a parent image\n# Choose a version compatible with your code and dependencies (e.g., LTS version)\nFROM node:18-slim\n\n# Set the working directory in the container\nWORKDIR /usr/src/app\n\n# Copy package.json and package-lock.json (if available)\n# Copying these first leverages Docker cache layers\nCOPY package*.json ./\n\n# Install app dependencies\nRUN npm install\n# If you were only installing production dependencies, you might use:\n# RUN npm ci --only=production\n\n# Bundle app source inside Docker image\nCOPY . .\n\n# Define environment variables (optional, but good practice for config)\n# Example: You could set these during 'docker run' instead of hardcoding in the script\n# ENV GOOGLE_PROJECT_ID=your-project-id\n# ENV BIGQUERY_DATASET_ID=your-dataset-id\n# ENV BIGQUERY_TABLE_ID=your-table-id\n# ENV SENSORPUSH_EMAIL=your-email\n# ENV SENSORPUSH_PASSWORD=your-password\n# ENV GOOGLE_APPLICATION_CREDENTIALS=/path/inside/container/to/keyfile.json\n\n# Define the command to run your app\nCMD [ \"node\", \"fetch-all-sensorpush-to-bq.js\" ] 